welcome() {

  vibe x;
  vibe y;

  x = 10;
  y = 2;

  x = x + y;
  x = x - y;
  x = x * y;
  x = x / y;

  x = -x;
  x = +x;


  ++x;
  --x;


  x++;
  x--;


  ify (x < y) {
    x = x + 1;
  } elfy {
    x = x - 1;
  }


  while (x < 20) {
    ++x;

    ify (x == 15) {
      break;
    }
  }

  take x;
}

