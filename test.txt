fun sum(){
    take 2;
}

welcome() {

// comment
  vibe x;
  vibe y;

  x = 10;
  y = 2;


  x = x + y;
  x = x - y;
  x = x * y;
  x = x / y;

  x = -x;
  x = +x;


  ++x;
  --x;


  x++;
  x--;


  ify (x < y) {
    x = x + 1;
  } elfy {
    x = x - 1;
  }


  while (x < 20) {
    ++x;

    ify (x == 15) {
      break;
    }
  }
  
  
  for (x = 0; x < 5; x++) {
    ++y;
  }

  take x;
}

