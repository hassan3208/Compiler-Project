fun sum(vibe a, vibe b){
    take a+b;
}

welcome() {

// comment
  vib x;
  vibe y;
  
  

  x = 10;
  y = 2;
  sum(a,b);

  x = x + y;
  x = x - y;
  x = x * y;
  x = x / y;

  x = -x;
  x = +x;


  ++x;
  --x;


  x++;
  x--;


  if (x < y) {
    x = x + 1;
  } elfy {
    x = x - 1;
  }


  while (x < 20) {
    ++x;

    ify (x == 15) {
      break;
    }
  }
  
  
  for (x = 0; x < 5; x++) {
    ++y;
  }

  //take x;
}

